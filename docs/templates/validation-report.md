# Validation Report — <scope / US-XXX>

- **Date:** YYYY-MM-DD
- **Scope:** <what was validated>

## Commands Run
```powershell
cd project
# python -m pytest tests/...        (reference config PATHS only — never paste secret/token values)
```

## Results
| Check | Command | Result | Evidence |
|-------|---------|--------|----------|
| <name> | `<cmd>` | not run | |

(Default every row to "not run"; change only with recorded output.)

## Evidence
<Paste relevant output snippets, exit codes, file paths, commit hash.>

## Gaps
- <what is still unproven / out of scope / deferred>
