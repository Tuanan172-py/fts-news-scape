# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

# Secrets — không bao giờ commit
config/secrets.yaml
.env

# Runtime data
logs/
data/*.db
data/*.db-wal
data/*.db-shm
data/raw/
data/dedup_cache.json
data/notifications/
data/exports/
# Single-instance lock cua run_daily.ps1
data/.pipeline.lock
# Handoff & pipeline artifacts (tái sinh được — KHÔNG commit)
# task-packets L1 + bóc tách (l1_route.py / agent_export.py)
data/agent_tasks/
# output agent bóc tách nộp về
data/agent_outputs/
# output agent L1 nộp về
data/agent_outputs_l1/
# Silver (dẫn xuất Bronze qua derive)
data/silver/
# work-package cho handoff
data/work_packages/
# Bronze raw HTML (nặng — tái scrape được)
data/raw_html/
# Bronze bao cao dinh ky NSO (HTML + .xlsx/.docx)
data/raw_reports/
# Report ngay do domain_check/daily_reporter sinh (output chay may)
data/reports/
# Log chay may cua morninger/daily (khong vao git)
daily_log.txt
