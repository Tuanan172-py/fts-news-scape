# News-Scape DSH patch — H1/H2 (D4: patch-only, chưa đóng gói bundle)
# Đây là bản nguồn trong repo. Bản được DSH nạp là:
#   %DSH_HOME%\profiles\web\cordis.patch.yml
# Ghi đè toàn bộ config của row, nên phải restate đủ trường.
- id: agent-presets
  config:
    default: ptc
    roots:
      - path: 'C:\Users\anpt\OneDrive - fpts.com.vn\FRA_DataIngestion - news-scape\.agents\dsh\presets'
        trust: user
