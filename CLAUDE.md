# CLAUDE.md — repo scope (news-scape)

@AGENTS.md

> This file is repo-scoped. It **extends** the global `~/.claude/CLAUDE.md`; it does not override it.
> Its only job is to import the harness entrypoint (`AGENTS.md`) so Claude Code loads the authority gate.
> Read `AGENTS.md` first, then the `docs/` harness as the phase needs.
